// Ендпоінти, за якими тест визначає бізнес-результат.
// Шляхи знає і спека (для перевірок), і QuizPage (щоб дочекатись відповіді), тому вони тут.
export const API = {
  // Створення користувача
  USERS: '/api/v1/users',
  // Підтвердження бронювання пробного уроку
  LESSONS: '/api/v1/lessons',
} as const;
