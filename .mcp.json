{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp",
        "--caps=network,storage",
        "--isolated",
        "--output-dir=variant2/artifacts"
      ]
    }
  }
}
